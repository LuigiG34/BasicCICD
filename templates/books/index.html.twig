<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Books</title>
  <style>
    body{font-family:system-ui,Segoe UI,Roboto,Arial,sans-serif;background:#0f172a;color:#e2e8f0;margin:0;padding:24px}
    h1{margin:0 0 20px 0}
    .grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:18px}
    .card{background:#111827;border:1px solid #374151;border-radius:12px;overflow:hidden}
    .cover{width:100%;height:300px;object-fit:cover;background:#0b1220}
    .pad{padding:12px}
    .title{font-weight:600}
    .author{opacity:.8;font-size:.9rem}
  </style>
</head>
<body>
  <h1>Books</h1>
  <div class="grid">
    {% for b in books %}
      <div class="card">
        <img class="cover" src="{{ b.coverUrl }}" alt="{{ b.title }}">
        <div class="pad">
          <div class="title">{{ b.title }}</div>
          <div class="author">{{ b.author }}</div>
        </div>
      </div>
    {% else %}
      <p>No books yet.</p>
    {% endfor %}
  </div>
</body>
</html>
